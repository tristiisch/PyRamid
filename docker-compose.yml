services:
  discord:
    build:
      dockerfile: ./Dockerfile
      target: executable
      context: .
    restart: always
    volumes:
      - ./src:/app/src
      - ./logs:/app/logs
      - ./songs:/app/songs
    networks:
      - pyramid_network
    env_file: .env

networks:
   pyramid_network:
    driver: bridge
